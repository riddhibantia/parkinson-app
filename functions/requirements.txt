# Runtime deps for Cloud Functions (Stage 6 / 4.5 / 4.6).
# Heavy training deps (torch, tabpfn, tabpfn-extensions) remain
# offline-only (Colab) and are not bundled — see functions/models/README.
# SHAP is deferred: if absent at runtime, fill_shap_explanation
# degrades to shap_status=unavailable without breaking the deploy.
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
shap>=0.44
firebase-admin>=6.0
firebase-functions>=0.3
google-cloud-storage>=2.0
pytest>=8.0
