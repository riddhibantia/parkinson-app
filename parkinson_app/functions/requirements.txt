# Stage 3 pipeline + Stage 4.1/4.3 scaffolding deps. Model-training deps
# (torch, tabpfn, tabpfn-extensions, shap, joblib) land in Stages 4-5.
# scikit-learn is here for metrics/splits/scalers only — no model fitting.
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
firebase-admin>=6.0
firebase-functions>=0.3
google-cloud-storage>=2.0
pytest>=8.0
